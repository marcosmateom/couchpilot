# Generated by ./mc setup — Intel QuickSync / AMD VAAPI hardware transcoding.
# Grants Jellyfin access to the GPU render node. Re-run ./mc setup to change.
services:
  jellyfin:
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - "__RENDER_GID__"
