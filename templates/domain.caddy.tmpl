# ══════════════════════════════════════════════════════════════════════════════
# Generated by ./mc setup — DO NOT EDIT (re-run ./mc setup to regenerate).
# HTTPS vhosts for domain: __DOMAIN__
# Certificates: Let's Encrypt via Cloudflare DNS-01 (no port forwarding needed).
# ══════════════════════════════════════════════════════════════════════════════

jellyfin.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy jellyfin:8096 {
        header_up Host {upstream_hostport}
    }
}

sonarr.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy sonarr:8989
}

radarr.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy radarr:7878
}

bazarr.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy bazarr:6767
}

prowlarr.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy prowlarr:9696
}

# qBittorrent validates Host and, with CSRF protection on, requires Origin to
# match — both pinned to the real hostname here. Set
# WebUI → Server domains: qbit.__DOMAIN__  (or *) in qBittorrent.
qbit.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy qbittorrent:8080 {
        header_up Host qbit.__DOMAIN__
        header_up Origin https://qbit.__DOMAIN__
    }
}

jellyseerr.__DOMAIN__,
requests.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy jellyseerr:5055
}

# ── Optional profiles (502 until enabled — harmless) ──────────────────────────

jackett.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy jackett:9117
}

rdt.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy rdtclient:6500
}

manga.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy suwayomi:4567
}

reader.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    reverse_proxy komga:25600
}

pihole.__DOMAIN__ {
    import site_common
    import tls_cloudflare
    redir / /admin/ 302
    reverse_proxy pihole:80
}
